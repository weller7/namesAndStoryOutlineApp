<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Character name list</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Character name list</h1>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> possible character names left.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter character name" name='todoItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>
    <a href="/plotPoints">Create your story outline</a>

    <script src="js/main.js"></script>
</body>
</html>